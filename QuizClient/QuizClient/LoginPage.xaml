<Page x:Class="QuizClient.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="LoginPage"
      Unloaded="Page_Unloaded"
      Background="{StaticResource AppBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- CARD CENTRALE -->
        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardStyle}" Width="400">
            <StackPanel>
                <!-- Titolo pagina -->
                <TextBlock Text="Generatore automatico di Test" Style="{StaticResource PageTitle}" TextAlignment="Center"/>

                <!-- Email -->
                <TextBlock Text="Email" Style="{StaticResource SectionTitle}" />
                <TextBox x:Name="EmailBox" Margin="0,0,0,10" Height="30" />

                <!-- Password -->
                <TextBlock Text="Password" Style="{StaticResource SectionTitle}" />
                <PasswordBox x:Name="PasswordBox" Margin="0,0,0,20" Height="30" />

                <!-- Pulsanti -->
                <Button x:Name="LoginButton" Content="Login" Click="Login_Click" Height="40" />
                <Button x:Name="RegisterButton" Content="Registrati" Click="Register_Click" Height="40" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
